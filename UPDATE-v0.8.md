# Update naar v0.8

1. Pak de release over de bestaande lokale repository uit.
2. Commit en push.
3. Doe op de VPS `git pull origin main`.
4. Open de homepage. Een database-reset of reparse is niet nodig.

Commitbericht:

`refactor(v0.8): introduce application core controllers services and repositories`
