<?php
declare(strict_types=1);

/**
 * Curated/manual inventory-icon overrides: Canonical Item Name => Gw.dat file id.
 *
 * The supplied extractor package contains authentic inventory icons but its
 * manifest does not yet contain item names/model IDs. These high-use currency
 * icons are pinned here so the dashboard is correct immediately. Additional
 * items can be linked from Admin -> Inventory icons without renaming files.
 */
return [
    'Glob of Ectoplasm' => 136300,
    'Armbrace of Truth' => 231154,
    'Zaishen Key' => 339663,
    'Obsidian Shard' => 285058,
];
