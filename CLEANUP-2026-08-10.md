# LittyWatch cleanup — 2026-08-10

Removed from this cleaned distribution:

- `.git/` — local Git repository metadata/history; not application runtime.
- Root `PHASE-*.md` files — historical build notes.
- Temporary parser probes: `_phase3l_probe.php`, `_phase3l_probe2.php`, `_probe_l1.php`, and old Phase 3X test README.
- `cron/v2-alerts.php` — exact duplicate of active `cron/evaluate-alerts.php`.
- Completed one-shot Phase 4C–4H installer/report scripts.
- Completed Phase 5A–5D maintenance directories.
- Superseded Armbrace manual-override installer scripts.

Intentionally retained:

- Application runtime (`app`, `routes`, `api`, `config`, root entrypoints).
- Database migrations.
- Assets and catalog/knowledge data.
- Parser regression tests (excluding temporary probes).
- Current collection/reparse/AI/training maintenance tools.
- Phase 5E–6B tooling, because this is the current active cleanup/classification workflow.
- `remove-armbrace-manual-override.php` as a safe rollback utility.

## Tweede opschoonronde: code-level cleanup

Verwijderd na statische verwijzingscontrole:

- `api/v2-*.php` — oude directe V2 API-endpoints; de huidige applicatie gebruikt `routes/api.php`.
- `app/V2/Controllers`, `Core`, `Repositories`, `Search`, `Trader`, `Encyclopedia`, `Views` — alleen gekoppeld aan de verwijderde oude V2 entrypoints/API's.
- `app/V2/bootstrap.php` en `app/V2/bootstrap-db.php` — oude bootstrap-stack.
- `app/V2/WatchlistService.php` en `app/V2/Services/ExchangeRateService.php` — vervangen door de huidige `app/Services` implementaties.
- `tools/v2-check.php` en `tools/v2-schema-check.php` — oude V2-stack checks.
- `app/Knowledge/JsonImporter.php` en `app/Knowledge/GwMarketDiscovery.php` — nergens meer aangeroepen.
- `app/Data/phase4e-items.json`, `phase4f-items.json`, `phase4h-items.json` — verweesde fase-data zonder runtime-referenties.

Bewust behouden:

- V2 `Assets`, `Intelligence`, `SnapshotService`, `MarketStats`, `Database`, `Schema`, `RuntimeStatus`, `Alerts/LiveFeedService` en `Alerts/AlertService`: deze hebben nog actieve gebruikers in controllers/cron.
- Parser/regressietests: hoewel veel fasegebonden namen hebben, vormen ze nog regressiedekking voor parsergedrag.
- Phase 5E t/m 6B maintenance tooling: momenteel nog onderdeel van de actieve datakwaliteitsworkflow.
