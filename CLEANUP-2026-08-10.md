# LittyWatch cleanup — 2026-08-10

Removed from this cleaned distribution:

- `.git/` — local Git repository metadata/history; not application runtime.
- Root `PHASE-*.md` files — historical build notes.
- Temporary parser probes: `_phase3l_probe.php`, `_phase3l_probe2.php`, `_probe_l1.php`, and old Phase 3X test README.
- `cron/v2-alerts.php` — exact duplicate of active `cron/evaluate-alerts.php`.
- Completed one-shot Phase 4C–4H installer/report scripts.
- Completed Phase 5A–5D maintenance directories.
- Superseded Armbrace manual-override installer scripts.

Intentionally retained:

- Application runtime (`app`, `routes`, `api`, `config`, root entrypoints).
- Database migrations.
- Assets and catalog/knowledge data.
- Parser regression tests (excluding temporary probes).
- Current collection/reparse/AI/training maintenance tools.
- Phase 5E–6B tooling, because this is the current active cleanup/classification workflow.
- `remove-armbrace-manual-override.php` as a safe rollback utility.
